spring:
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        group: APPLICATION_CONFIG
        name: ceap-admin
        # dev
        namespace: ceap